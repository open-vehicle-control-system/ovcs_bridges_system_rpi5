# Sensor drivers for libcamera. These match `dtoverlay=imx708,cam{0,1}`
# entries in config.txt; loading them at boot makes rp1-cfe bind the
# Pi 5 CSI subdevices so libcamera enumerates the cameras without a
# manual `modprobe` after boot.
imx708
